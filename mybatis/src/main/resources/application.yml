# 配置端口
server:
  port: 10011
spring:
  # 配置数据源
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.1.15:3306/ivt-city?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8&zeroDateTimeBehavior=convertToNull
    username: root
    password: Aa123456
    type: com.alibaba.druid.pool.DruidDataSource

mybatis:
  mapper-locations: classpath*:dao/*.xml
  type-aliases-package: com.watimer.study.mybatis.dao

#logging
logging:
  level:
    com.watimer.study.mybatis.dao: debug
